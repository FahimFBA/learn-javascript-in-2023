const firstName = "Fahim";
const job = "Student";
const birthYear = 1000;
const year = 2023;
const fahim =
  "I'm " + firstName + ", a " + (year - birthYear) + " years old " + job + "!";
console.log(fahim);

const fahimNew = `I'm ${firstName}, a ${year - birthYear} years old ${job}!`;

console.log(fahimNew);

console.log(`Just a regular string...`);

console.log(`String with \n\
multiple \n\
lines`);

console.log(`String
multiple
lines`);
